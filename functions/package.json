{
  "name": "sitetemplate2025-functions",
  "description": "Firebase Functions for SiteTemplate2025",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0",
    "sharp": "^0.33.0",
    "png-to-ico": "^2.1.8"
  },
  "devDependencies": {
    "typescript": "^5.1.0",
    "@types/node": "^20.0.0"
  },
  "private": true
}